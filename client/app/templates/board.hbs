<h2>{{model.title}}</h2>
<div class="row">
    <div class="lists">
      {{#each list in model.lists}}
          {{board-list list=list }}
      {{/each}}
      <div class="list create-list">
        {{partial "partials/list-inline-model-creator"}}
      </div>
    </div>
</div>
{{outlet}}
